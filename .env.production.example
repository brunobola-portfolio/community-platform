# ==============================================================================
# ARCVA 2.0 - Production Environment (EXAMPLE)
# ==============================================================================
# Copy this to .env.production and fill in the real values.
# Used by `npm run build`. Secrets (GEMINI_API_KEY) live on the Convex server,
# never in this file — only public VITE_* values are safe here.
# ==============================================================================

# Convex production deployment (get it from `npx convex dashboard --prod`)
VITE_CONVEX_URL=https://savory-bird-627.convex.cloud

# Site identity
VITE_SITE_NAME=ARCVA
VITE_CONTACT_EMAIL=geral@arcva.pt
VITE_MAINTENANCE_MODE=false
VITE_LOGO_URL=/favicon.svg

# Contact fallbacks (runtime values come from Admin > Definições)
VITE_ADDRESS=Largo do Pavilhão, N° 1, 2395-301 Minde, Vale Alto
VITE_PHONE=+351 249 979 123
VITE_MAPS_URL=https://maps.app.goo.gl/zzqN8LJMgkFKd2mn9

# Chatbot defaults (override via Admin > IA & Chatbot)
VITE_ENABLE_CHATBOT=true
VITE_SHOW_CHATBOT_BUBBLE=true
VITE_CHAT_MODEL=gemini-2.0-flash
VITE_CHAT_MODEL_FALLBACK=gemini-2.0-flash-lite
VITE_THINKING_BUDGET=512

# AI content defaults
VITE_DEFAULT_IMAGE_STYLE=Cinematic lighting, photorealistic, 4k, community atmosphere, warm tones
VITE_IMAGE_MODEL=gemini-2.0-flash-exp
VITE_CONTENT_TONE=Profissional e Inspirador
VITE_TTS_MODEL=gemini-2.5-flash-preview-tts
VITE_IMAGE_RESOLUTION=1k
VITE_AI_GUARDRAILS_ENABLED=true

# Location & social fallbacks
VITE_LATITUDE=39.515469
VITE_LONGITUDE=-8.586681
VITE_FACEBOOK_PAGE_ID=arcvalealto

# Server-side no Convex (npx convex env set --prod NOME valor):
#   GEMINI_API_KEY (obrigatoria com provider gemini)
#   Opcional multi-provedor: AI_PROVIDER, OPENROUTER_API_KEY, OPENROUTER_MODEL,
#   CUSTOM_AI_URL, CUSTOM_AI_API_KEY, CUSTOM_AI_MODEL
#   (tambem configuravel em Admin > IA & Chatbot, que tem precedencia)
