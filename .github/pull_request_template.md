## What

<!-- One or two sentences: what does this PR change and why. -->

## Checklist

- [ ] `npm run type-check`, `npm run lint` and `npm run build` pass
- [ ] No `any`, no inline styles, no hardcoded association identity (settings-first — see docs/WHITE-LABEL.md)
- [ ] Admin/user mutations carry `requireAdmin`/`requireAuth` on the first line
- [ ] UI changes verified in light and dark themes
- [ ] Docs updated if behavior or commands changed
